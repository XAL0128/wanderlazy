# wanderlazy

旅行手账网页版，从微信小程序迁移而来，通过 GitHub Pages 发布。

## 本地预览

直接用浏览器打开 `index.html`，或用任意静态服务器：

```bash
python3 -m http.server 8000
```

## 目录结构

- `index.html` — 页面结构
- `css/style.css` — 样式（配色沿用小程序的水彩纸质风格）
- `js/` — 数据与交互逻辑（后续加入）
- `assets/` — 图片与装饰素材（后续加入）
